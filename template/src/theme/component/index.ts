import themeButtonConfig from './Button.config';
import themeSpinnerConfig from './Spinner.config';
import themeStackConfig from './Stack.config';
import themeTextConfig from './Text.config';

const themeComponentsConfig = {
  Button: themeButtonConfig,
  Stack: themeStackConfig,
  Text: themeTextConfig,
  Spinner: themeSpinnerConfig,
};

export default themeComponentsConfig;
